name: PowerShell check

on:
  push:
  pull_request:

jobs:
  parse:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run no-Excel test suite
        shell: pwsh
        run: ./tools/tests/Run-AllTests.ps1
